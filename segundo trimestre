const precosLivros = [25, 15, 30, 50, 45, 20];

let maisBrato = 0; 

for(let atual = 0; atual < precosLivros.length, atual++) { 
    if (precosLivros[atual] < precosLivros[maisBarato] ) {
        maisBarato = atual
    }
}
console.log(`o livromais barato custa ${precosLivros[maisBarato]}`)
